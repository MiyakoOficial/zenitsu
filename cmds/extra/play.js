module.exports = {
    config: {
        name: "buylevel", //nombre del cmd
        alias: ['buyl'], //Alias
        description: "Comprar niveles", //Descripción (OPCIONAL)
        usage: "z!buylevel num",
        category: 'extra',
        botPermissions: [],
        memberPermissions: []

    }, run: async ({ client, message, args, embedResponse }) => {


        /*
        
        let res;
        for(let i = 0;i < 5;i++){
        res = await descargar(`https://www.youtube.com/watch?v=jItnCGRsMjw`).catch(()=>{});
        if(res) break;
        await Discord.Util.delayFor(2000)
        }
        return res
        
        */

    }
}